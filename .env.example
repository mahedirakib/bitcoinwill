# Bitcoin Will - Environment Variables Template
# Note: Since this is a client-side only app, variables prefixed with VITE_ 
# will be exposed in the frontend bundle.

# Optional: Custom Mempool.space API endpoint (useful for self-hosting or local regtest)
# VITE_MEMPOOL_API_URL=https://mempool.space/api

# Network Toggle (testnet or mainnet)
VITE_BITCOIN_NETWORK=testnet
